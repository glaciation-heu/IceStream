apiVersion: v1
kind: Service
metadata:
  name: cacheservice
spec:
  selector:
    app.kubernetes.io/name: cacheservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: { { .Values.service.type } }
